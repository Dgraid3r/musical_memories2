# Optional - error tracking (Sentry). Leave unset to disable entirely (no
# effect on the app either way). Copy this file to frontend/.env and fill
# in to enable - Vite only exposes vars prefixed VITE_ to client code.
VITE_SENTRY_DSN=
